{
  "name": "ml-review-summary-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless proxy that summarizes MercadoLibre reviews with Gemini 2.5 Flash (Pilar 2). The GEMINI_API_KEY lives ONLY here as a Vercel env var.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
